"use strict";

/**
 * fsRepoUrl
 * Get the repository url from a path to a repo.
 *
 * @name fsRepoUrl
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function fsRepoUrl (a, b) {
    return a + b;
};